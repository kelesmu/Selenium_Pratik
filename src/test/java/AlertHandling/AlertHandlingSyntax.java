package AlertHandling;

import org.openqa.selenium.Alert;

public class AlertHandlingSyntax {


 /*   driver.findElement(tryItLocator).click();
    Alert myAlert = driver.switchTo().alert();
        System.out.println(myAlert.getText());
        myAlert.accept();
    */
}
